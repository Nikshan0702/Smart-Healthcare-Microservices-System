apiVersion: v1
kind: Service
metadata:
  name: telemedicine-service
  labels:
    app: telemedicine-service
spec:
  selector:
    app: telemedicine-service
  ports:
    - name: http
      protocol: TCP
      port: 5007
      targetPort: 5007
  type: ClusterIP
