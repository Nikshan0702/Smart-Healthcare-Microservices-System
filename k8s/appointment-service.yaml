apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  labels:
    app: appointment-service
spec:
  selector:
    app: appointment-service
  ports:
    - name: http
      protocol: TCP
      port: 5003
      targetPort: 5003
  type: ClusterIP
